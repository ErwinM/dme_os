#include "types.h"
#include "fs.h"
// ls.c

int
main()
{
	int fd;

	printf(1, "LS is running!\n");

	// ls should list the contents of the current dir for now
	// open the cwd for reading
	// read each entry
	// list it
	fd = open("/", 0);





	breek();
	exit();
}